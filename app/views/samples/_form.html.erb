<%= nested_form_for @sample do |f| %>
  <% if @sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sample.errors.count, "error") %> prohibited this sample from being saved:</h2>

      <ul>
      <% @sample.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div id='section_1'>
    <%= f.label "Date" %>
    <%= f.date_select :sample_date %><br />

    <%= render "diver_selection", :object => f %>


    <%= f.label "Sample Type" %>
    <%= f.collection_select :sample_type_id, SampleType.all, :id, :sample_type_name, { :include_blank => "" } %><br />

    <%= f.label "Habitat Type" %>
    <%= f.collection_select :habitat_type_id, HabitatType.all, :id, :habitat_name, { :include_blank => "" } %><br />

    <%= f.label "Dive Begin Time" %>
    <%= f.time_select :dive_begin_time, { :include_blank => "" } %><br />

    <%= f.label "Dive End Time" %>
    <%= f.time_select :dive_end_time, { :include_blank => "" } %><br />
  </div>
  <div id='section_2'>
    <%= f.fields_for :sample_animals do |builder| %>
      <%= builder.label "Species" %>
      <%= builder.collection_select :animal_id, Animal.all, :id, :common_name, {:include_blank => ""} %>
      <%= builder.label "N" %>
      <%= builder.number_field :number_individuals %>
      <%= builder.label "Mean" %>
      <%= builder.number_field :average_length %>
      <%= builder.label "Min" %>
      <%= builder.number_field :min_length %>
      <%= builder.label "Max" %>
      <%= builder.number_field :max_length %>
      <%= builder.link_to_remove "Remove Species"  %>

    <% end %>

    <p> <%= f.link_to_add "Add species", :sample_animals %></p>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
