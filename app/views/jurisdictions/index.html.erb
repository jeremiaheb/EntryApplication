<div class="grid-row">
  <div class="grid-col-12">
    <%= render "shared/nav_breadcrumbs", object: [["Jurisdictions", ""]] %>

    <ul class="usa-button-group margin-bottom-1">
      <li class="usa-button-group__item">
        <%= link_to new_jurisdiction_path, class: "new-jurisdiction-button usa-button" do %>
          <svg class="usa-icon" aria-hidden="true" focusable="false" role="img">
            <%= tag.use href: image_path("sprite.svg#add") %>
          </svg>
          New Jurisdiction
        <%- end %>
      </li>
    </ul>

    <table class="width-full display">
      <thead>
        <tr>
          <th scope="col">Name</th>
          <th scope="col">Valid Habitat Types</th>
          <th scole="col"></th>
        </tr>
      </thead>
      <tbody>
        <%- @jurisdictions.each do |jurisdiction| %>
          <tr>
            <td><%= jurisdiction.name %></td>
            <td><%= jurisdiction.habitat_types.pluck(:habitat_name).sort.join(", ") %></td>
            <td>
              <ul class="usa-button-group float-right">
                <li class="usa-button-group__item">
                  <%= link_to "Edit", edit_jurisdiction_path(jurisdiction), class: "usa-button usa-button--outline" %>
                </li>
                <li class="usa-button-group__item">
                  <%= link_to "Delete", jurisdiction, data: { confirm: "Are you sure?" }, method: "delete", class: "usa-button usa-button--secondary" %>
                </li>
              </ul>
            </td>
          </tr>
        <%- end %>
      </tbody>
    </table>
  </div>
</div>