<h1>Listing habitat_types</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @habitat_types.each do |habitat_type| %>
  <tr>
    <td><%= link_to 'Show', habitat_type %></td>
    <td><%= link_to 'Edit', edit_habitat_type_path(habitat_type) %></td>
    <td><%= link_to 'Destroy', habitat_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Habitat type', new_habitat_type_path %>
